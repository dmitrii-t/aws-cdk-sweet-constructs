import { App } from '@aws-cdk/core';

// Unit test for the construct
describe('should generate a cfn template with a topic backed by queue', () => {
  // Given
  const app = new App();

  // When
  // const stack = new TopicQueueWithHandler(app, ' QueueHandlerTestStack');

  // Then
  // expectCDK(stack).to(matchTemplate({
  //   Resources: {
  //
  //   }
  // }, MatchStyle.EXACT));
});
